AddDefaultCharset UTF-8
 
Options -Indexes
Options FollowSymlinks
 
RewriteEngine On
 
RewriteCond %{REQUEST_URI} ^/(rway/admin)
RewriteRule ^admin(\/?.*)$ backend/web/$1 [L]
 
RewriteCond %{REQUEST_URI} ^/(rway)
RewriteRule ^(\/?.*)$ frontend/web/$1 [L]
